import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    height: 50,
    width: 200,
  },
  title1: {
    fontSize: 30,
    color: "#c3c3",
  },
});

export default styles;
